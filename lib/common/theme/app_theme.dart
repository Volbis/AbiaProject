import 'package:flutter/material.dart';

class AppColors {
  // Couleurs principales
  static const primaryColor = Color.fromARGB(239, 249, 83, 32);
  static const accentColor = Color(0xFF0B735F);
  
  // Couleurs du dégradé
  static const gradientStart = Color(0xFFF9541F);
  static const gradientEnd = Color(0xFFFE6F42);
  
  // Couleurs neutres
  static const textPrimary = Color(0xFF2A3037);
  static const textSecondary = Color(0xFFAAACAE); 
  static const dividerColor = Color(0xFFDFE2E5);
  
  // Couleurs d'arrière-plan
  static const backgroundColor = Color(0xFFF7F7FB);
  static const cardBackground = Color(0xFFFCFCFC);
  static const surfaceColor = Color(0xFFFFFFFF);
}